# ~/.bash_aliases — 用户个性化 aliases（仅当文件不存在时由 chezmoi 创建）
# 通用 aliases 统一在 ~/.bash_components/bashrc.d/*.sh 中维护。
# 这里用于每台设备上的个人补充；编辑后不会被 chezmoi apply 覆盖。

# 示例：
# alias rgs='rg -n --hidden --glob "!.git"'
